<div class="container my-5">
  <h1>📋 <%= @list.name %></h1>
    <div>
      <ul>
        <% @list.movies.each do |movie| %>
          <li><%= movie.title %></li>
        <% end %>
      </ul>
    </div>

  <p><%= link_to "➕ Create a new movie list here!", new_list_path, class: "text-decoration-none" %></p>
  <div class="mt-3">
    <p><%= link_to '👉 Bookmark movies to this list', new_list_bookmark_path(@list), class: 'btn btn-success' %></p>
    <p><%= link_to '👈 Back to all lists', lists_path, class: 'btn btn-secondary' %></p>
  </div>
</div>

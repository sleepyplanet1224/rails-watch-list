<div class="container my-5">
  <div class="px-5 py-3 my-3">
    <h1 class="text-center semibold">✍️ Create your new list!</h1>
    <%= simple_form_for @list do |f| %>
      <%= f.input :name %>
      <%= f.input :photo, as: :file, input_html: { multiple: true } %>
      <%= f.submit("🩷 Add your new list", class: 'btn btn-success') %>
    <% end %>

    <div class="mt-3">
      <p><%= link_to '👈 Back to all lists', lists_path, class: 'btn btn-secondary' %></p>
    </div>
  </div>
</div>
